class AnimationConstants {
  static const Duration defaultDuration = Duration(milliseconds: 250);
  static const Duration fadeTransition = Duration(milliseconds: 250);
  static const Duration slideTransition = Duration(milliseconds: 250);
  static const Duration testDuration = Duration(milliseconds: 1000);
}
